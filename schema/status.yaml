title: Status
type: object

properties:
  level:
    type: string
    enum:
    - pipeline
    - stage
    - step
  matrix:
    type: string
    enum:
    - itemize
    - summarize
  disabled:
    type: boolean
