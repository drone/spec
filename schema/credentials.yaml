type: object
title: Credentials

properties:
  username:
    type: string
  password:
    type: string
  aws:
    $ref: ./credentials_aws.yaml
  

# container:
#   image: golang
#   connector: account.dockerhub
#   username: $DOCKER_USERNME
#   password: $DOCKER_PASSWORD
#   aws:
#     access_key: $AWS_ACCESS_KEY
#     secret_key: $AWS_SECRET_KEY
#     oidc_role:  $AWS_OIDC_ROLE
