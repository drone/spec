title: Failure
type: object
properties:
  action:
    oneOf:
    - $ref: "./failure_action.yaml"
    - type: array
      items:
        $ref: "./failure_action.yaml"

# skip and manually create go file
x-go-skip: true
